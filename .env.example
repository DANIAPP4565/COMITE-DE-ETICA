# Valores por defecto para correr en tu máquina. Sirven tal cual para probar.
# Para PRODUCCIÓN: cambia contraseñas y genera una clave maestra nueva.

# --- PostgreSQL ---
POSTGRES_USER=cei
POSTGRES_PASSWORD=cei
POSTGRES_DB=cei

# --- Datos de demostración (usuarios y credenciales de prueba) ---
SEED_DEMO_DATA=true

# --- Almacenamiento S3 (MinIO local) ---
S3_BUCKET=cei-documents
S3_ACCESS_KEY_ID=cei-access
S3_SECRET_ACCESS_KEY=cei-secret-key

# --- Clave maestra para cifrar documentos (32 bytes en Base64) ---
# Clave de DESARROLLO generada automáticamente. CÁMBIALA en producción con:
#   python -c "import os,base64;print(base64.b64encode(os.urandom(32)).decode())"
DOCUMENT_MASTER_KEY_B64=EYRatV4g6k5ej5POL/S22q2Z6Nl2CrZDqjlxIU+oJqY=
